# 验证记录

2026-09-21，Minecraft 1.20.1 / Forge 47.3.7。

0.3.0 执行 `./gradlew -Pverification runGameTest`（Windows 使用 `gradlew.bat`），真实 Forge 游戏测试服务器输出：`All 4 required tests passed :)`。基础行为测试包含以下断言：

- 普通和高压刷怪蛋分别对应正确的实体类型；普通实体初始未强化，高压实体初始强化。
- 对普通实体调用真实雷击处理后进入强化状态；写入 NBT 并恢复到新实例后保持强化。
- 高压攻击立即产生一次强度 6 的爆炸；重复调用不会再次引爆同一实体。
- 普通攻击启动引信，计时结束前不爆炸；结束时产生一次强度 3 的爆炸。
- 将替换概率暂设为 100% 后，自然生成的原版幻翼恰好替换为一只新实体，命令生成的幻翼不被替换。
- 将轰炸概率暂设为 100% 后，露天箱子能被发现；遮挡后终止追击且不能重新锁定。
- `mobGriefing=false` 禁止容器轰炸；实际触发高压爆炸仍伤害附近实体，但箱子不被破坏。

爆炸强度/引信测试监听并取消实际爆炸以隔离测试对象；最后的游戏规则测试执行真实爆炸。临时概率与游戏规则在测试结束后恢复。验证类和测试结构只在显式传入 `-Pverification` 时编译，默认构建不包含。

0.2.0 新增 GT 联动测试，使用仅存在于验证源码中的接口替身与一个带物品槽位的方块实体，检查：

- 未安装 GT 时，可选接口自动关闭，不影响独立运行。
- 区分无关对象、未成型控制器和已成型控制器；控制器的物品接口不会使其额外进入容器袭击路径。
- 机器袭击概率为 0、联动关闭或 `mobGriefing=false` 时，不发起机器袭击。
- 存在可见且可攻击的玩家时，100% 测试概率下机器目标能抢占飞行目标，并保持追击。
- 控制器顶部被外壳挡住、但侧面仍露天可达时，仍可接近该控制器。
- 控制器失去成型状态或被移除后，停止追击。

适配接口名称和签名已对照 GTCEu 1.4.4 的本地 JAR 检查，使用 `IMachineBlockEntity.getMetaMachine()` 与 `IMultiController.isFormed()`。测试中的接口替身只用于验证适配逻辑和目标选择，不会打包进模组，也不能替代在完整 GTL/Additions 环境中的联动实测。

0.3.0 新增末影行为与生成配置测试，并扩展 GT 测试：

- 末影苦力怕普通/高压刷怪蛋类型正确，保留怕水与原版注视判断，雕刻南瓜阻止注视激怒。
- 无敌对目标时不发起玩家瞬移攻击，但能主动选择露天箱子；不需要先注视或攻击，也不会凭空产生玩家仇恨。
- 普通容器瞬移攻击启动引信，20 tick 后产生强度 3 的爆炸，引信可保存恢复；高压攻击瞬移后立即产生强度 6 的爆炸。
- Forge 取消瞬移时既不移动也不引爆，冷却后可重试；落点空间与液体检查会拒绝低矮天花板和水中位置。
- 实际雷击处理产生高压状态，NBT 恢复后保留。
- 使用控制器接口替身时，即使已有敌对玩家，末影形态仍能优先瞬移到已成型控制器附近点燃引信。
- 两种自然生成开关分别保留原版生物；关闭开关不阻止直接生成自定义生物。
- 倍率 0 保留原版，倍率 1 替换一个；倍率 2 在足够空间中实际生成两只幻翼。固定随机种子的 1.5 倍率会同时产生 1 和 2，比例在测试范围内。

测试以分批运行和内存配置隔离临时设置，避免频繁写盘触发 Forge 配置监听；高空测试场景会清理残留方块。测试直接驱动部分目标与事件以稳定复现边界，没有用完整客户端模拟玩家操作。

资源 JSON 已解析检查，中文翻译与四种刷怪蛋模型均存在。正式交付前执行不含验证代码的 `clean build` 并检查 JAR 内容。

此前已收到苦力幻翼基础版本游戏内安装测试可用的反馈。新增末影形态的客户端外观与完整 GTL/Additions 联动仍需实机确认。尚未逐一验证不同第三方容器模组。模组容器支持通过 `Container` 或 Forge `ITEM_HANDLER` 接口实现；不具备这些接口的特殊容器不在识别范围内。自然生成测试验证了生成事件替换链路，并未模拟玩家连续失眠数日；失眠生成条件由原版幻翼生成器负责。
